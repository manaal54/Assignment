var unit:number = 100;

if(unit >= 100 && unit < 200){
    var taxPrice:number = unit + (unit * 10)/100;
    console.log(`The Price of ${unit} units is ${taxPrice}`);
}
else if(unit >= 200 && unit < 500){
    var taxPrice:number = unit + (unit * 15)/100;
    console.log(`The Price of ${unit} units is ${taxPrice}`);
}
else{
    var taxPrice:number = unit + (unit * 25)/100;
    console.log(`The Price of ${unit} units is ${taxPrice}`);
}

var unit2:number = 300;

if(unit2 >= 100 && unit2 < 200){
    var taxPrice:number = unit2 + (unit2 * 10)/100;
    console.log(`The Price of ${unit2} units is ${taxPrice}`);
}
else if(unit2 >= 200 && unit2 < 500){
    var taxPrice:number = unit2 + (unit2 * 15)/100;
    console.log(`The Price of ${unit2} units is ${taxPrice}`);
}
else{
    var taxPrice:number = unit2 + (unit2 * 25)/100;
    console.log(`The Price of ${unit2} units is ${taxPrice}`);
}

var unit3:number = 510;

if(unit3 >= 100 && unit3 < 200){
    var taxPrice:number = unit3 + (unit3 * 10)/100;
    console.log(`The Price of ${unit3} units is ${taxPrice}`);
}
else if(unit3 >= 200 && unit3 < 500){
    var taxPrice:number = unit3 + (unit3 * 15)/100;
    console.log(`The Price of ${unit3} units is ${taxPrice}`);
}
else{
    var taxPrice:number = unit3 + (unit3 * 25)/100;
    console.log(`The Price of ${unit3} units is ${taxPrice}`);
}
